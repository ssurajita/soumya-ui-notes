Write a program for fibonacci series.
  var i;var fib = [];
  fib[0] = 0;fib[1] = 1;fib[2] = 1;
  function getFibonacci(n){
	  for(i=3;i<=n;i++){
		  fib[i] = fib[i-2]+fib[i-1];
	  }
  }
  console.log(fib);
  
Write a program for palindrome number/string.
